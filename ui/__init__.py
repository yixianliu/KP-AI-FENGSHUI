"""
UI 层包（PySide6 桌面界面）。

本包存放全部与界面呈现相关的代码，位于三层架构（UI -> core 业务层 -> SQLite）
的最上层，只负责「收集输入、展示结果」，不承载排盘与命理推演等业务规则：

  - main_window.py  主窗口与标签页调度，串联左侧输入面板和右侧结果面板
  - components/     可复用界面组件（输入面板、结果面板、卡片、对话框等）
  - export/         排盘结果导出器（PDF / Excel / CSV）
  - styles.py       全局配色、字号与样式表常量，保证各面板视觉统一

界面可见文案统一将 AI 品牌化表述为「龙虎山大师兄」，代码标识符仍用技术原名。
"""
